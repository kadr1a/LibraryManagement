<Window x:Class="LibraryManagement.Views.ManageAuthorsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление авторами" Height="450" Width="600"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <Button x:Name="AddButton" Content="Добавить" Width="80" Height="30" Margin="5" Click="AddButton_Click"/>
            <Button x:Name="EditButton" Content="Редактировать" Width="80" Height="30" Margin="5" Click="EditButton_Click"/>
            <Button x:Name="DeleteButton" Content="Удалить" Width="80" Height="30" Margin="5" Click="DeleteButton_Click"/>
        </StackPanel>

        <DataGrid x:Name="AuthorsDataGrid" Grid.Row="1" 
                  AutoGenerateColumns="False" IsReadOnly="True"
                  SelectionMode="Single" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Имя" Binding="{Binding FirstName}" Width="100"/>
                <DataGridTextColumn Header="Фамилия" Binding="{Binding LastName}" Width="100"/>
                <DataGridTextColumn Header="Дата рождения" Binding="{Binding BirthDate, StringFormat={}{0:dd.MM.yyyy}}" Width="120"/>
                <DataGridTextColumn Header="Страна" Binding="{Binding Country}" Width="150"/>
            </DataGrid.Columns>
        </DataGrid>
        <Button Grid.Row="2" Content="Закрыть" Width="80" Height="30" HorizontalAlignment="Right" Click="CloseButton_Click"/>
    </Grid>
</Window>